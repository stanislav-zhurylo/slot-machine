export default class Icon {
    code: string;
    imageLocation: string;

    constructor(code: string, imageLocation: string) {
        this.code = code;
        this.imageLocation = imageLocation;
    }
}